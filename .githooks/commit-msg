#!/bin/sh

if command -v python >/dev/null 2>&1; then
  python scripts/validate_commit_message.py "$1"
  exit $?
fi

if command -v py >/dev/null 2>&1; then
  py -3 scripts/validate_commit_message.py "$1"
  exit $?
fi

echo "提交校验失败：未找到可用的 Python 解释器（python / py）。" >&2
exit 1
